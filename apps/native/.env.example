# Where the Next.js backend (apps/web) is reachable FROM THE PHONE.
# Google OAuth needs a real https host, so use your ngrok domain here and as
# NEXTAUTH_URL in apps/web/.env. A free static ngrok domain keeps it stable.
EXPO_PUBLIC_BACKEND_URL=https://your-domain.ngrok-free.app
